# Database (matches compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=issueflow
DB_PASSWORD=issueflow
DB_NAME=issueflow

# Auth
JWT_SECRET=change-me-in-prod
JWT_EXPIRES_IN=3600

# App
PORT=3000
